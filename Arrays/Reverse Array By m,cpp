#include <bits/stdc++.h> 
void reverseArray(vector<int> &arr , int m)
{
	int s = m+1;
	int n = arr.size();
	int e = n-1;

	while(s <= e){
		
	int temp = arr[s];
		arr[s] = arr[e];
		arr[e] = temp;
		//You Can Use Swap Function This Is Explains Swap;
		s++;
		e--;
	}
}
